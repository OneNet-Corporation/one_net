part of 'theme.dart';

class AppIcon {
  AppIcon._();
  /// A collection of custom icons used in the application, sourced from the
  /// 'TwitterIcon' font family.
  ///
  /// To define an icon, use the `IconData` constructor with the appropriate
  /// code point and font family. For example:
  /// ```dart
  /// static const IconData exampleIcon = IconData(0xf123, fontFamily: 'TwitterIcon');
  /// ```

}