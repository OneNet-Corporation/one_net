part of 'theme.dart';

/// A class that defines the color palette for the application.
///
/// Each color is defined as a static constant of type [Color].
/// The colors are represented using hexadecimal values.
///
/// Example:
/// ```dart
/// Color myColor = AppColor.primary;
/// ```

class AppColor {
  static const Color primary = Color(0xFF153AFE);
  static const Color inactive = Color(0xFF667085);
  static const Color appBarIconBack = Color(0xFFF9FAFB);
  static const Color appBarIconBadgeBack = Color(0xFFEB5757);
  static const Color appBardivider = Color(0xFFF6F7F9);
  static const Color storyName = Color(0xFF1D2939);
  static const Color postCardIcon = Color(0xFF667085);
  static const Color white = Color(0xFFffffff);
}
